# Handoff v43 — пять пунктов улучшений за один билд

**Дата**: 2 мая 2026
**Демо**: https://konstruktor-stroenii.ru/
**Бранч**: пакетное обновление поверх v42
**Статус CI**: `flutter analyze lib` — 0 ошибок / 0 warning, `flutter test` — 14/14 passed

---

## Что сделано

Все 5 выбранных пунктов закрыты в одном билд-цикле, без промежуточных деплоев.

### П.3 — Проверка проёмов на стене (≤ 60% длины стены)

**Стандарт**: СП 31-105-2002 п. 5.4 (расчётная длина простенков), СП 31-114-2004 п. 6.3.

**Изменения**:
- Добавлена валидация в `floor_plan_generator.dart`: после расстановки окон/дверей суммарная длина проёмов на каждой стене не должна превышать 60 % от длины стены.
- Если правило нарушается — пропорционально уменьшаются ширины окон или удаляется крайнее окно/дверь до выполнения нормы.
- Добавлен unit-тест в `test/floor_plan_generator_test.dart`: «openings on wall ≤ 60% of wall length».

**Затронутые файлы**:
- `lib/services/floor_plan_generator.dart`
- `test/floor_plan_generator_test.dart`

---

### П.4 — Расход материалов по ФССЦ-2020

**Стандарт**: ФССЦ-2020 (нормы расхода) + ГЭСНм-12 (отходы).

**Изменения**:
- В таблице 4 «Стеновые материалы» (на листе спецификации) теперь показывается реальный расход с учётом штучного количества и отходов:
  - **Кирпич полнотелый** (250×120×65) — 410 шт./м³ × 1.05 (бой 5 %)
  - **Кирпич пустотелый** (250×120×88) — 380 шт./м³ × 1.05
  - **Газобетон D500/D600** (600×300×250) — 22 блок./м³ × 1.05
  - **Пенобетон** (600×300×200) — 28 блок./м³ × 1.05
  - **Керамзитобетон** (390×190×188) — 72 блок./м³ × 1.05
  - **Брус / клееный брус / бревно** — м³ × 1.07 (раскрой 7 %)
  - **Каркас SIP** — м² панелей × 1.05
- Кровля: профлист м² × 1.12; металлочерепица × 1.10; гибкая черепица × 1.10 + пачки по 3 м²; керамика 11 шт./м² × 1.07; цементно-песчаная 10 шт./м² × 1.07; рулонная битумная × 1.15 + рулоны по 10 м²; ПВХ/ТПО × 1.10.
- Утеплитель: эковата (целлюлоза) кг × 1.05 (плотность 50 кг/м³); стекловата м³ × 1.07; минвата/EPS м³ × 1.05.

**Где смотреть в PDF**: лист «Спецификация. Таблица 4. Стеновые материалы» — колонка «Расход (с отходами)».

**Затронутые файлы**:
- `lib/services/pdf_builder_materials.dart` (новый файл; см. п.9)
  - `wallMaterialConsumption()`
  - `roofMaterialConsumption()`
  - `insulationConsumption()`

---

### П.1 — Расчёт R-сопротивления по слоям пирога + сравнение с R_норм

**Стандарт**: СП 50.13330.2024 п.5.2, табл. 3 (нормируемое сопротивление теплопередаче), СП 131.13330.2020 табл. 3.1 (климатические зоны).

**Изменения**:
1. **Климатические зоны**: набор расширен с 5 до 12+ городов (`packages/cc_engine/lib/src/thermal.dart`):
   - Москва, СПб, Екатеринбург, Новосибирск, Краснодар (было раньше)
   - Казань, Нижний Новгород, Якутск, Сочи, Мурманск, Владивосток, Иркутск (добавлено)
   - Каждая зона: `tExtJan` (расч. температура), `tIn` (внутр.), `heatingDays`, `tHeatAvg`.
2. **Авто-подбор толщины утеплителя**:
   - Раньше толщина была захардкожена (200 мм для каркаса, 100 мм для кирпича и т.д.).
   - Теперь толщина считается из формулы СП 50 п. 5.1:
     `δ_утеп ≥ λ_утеп · (R₀ᵗᵖ − 1/αв − ΣRᵢ − 1/αн)`
     где R₀ᵗᵖ — нормируемое сопротивление по региону, ΣRᵢ — сумма R базовых слоёв.
   - Учтены типы стен: каркас, кирпич, газобетон, керамзитобетон, бревно, брус.
   - Учтены типы кровли: 4 базовых пирога + утеплитель.
3. **Энергопаспорт**: добавлен новый раздел «6. Энергопаспорт. Годовые теплопотери» в `thermal_explanation_pdf.dart`:
   - Площадь стен = периметр × высота × этажность × 0.88 (12 % остекления).
   - Площадь кровли = ширина × длина × 1.10 (свес и уклон).
   - Q_walls = (tвн − tот.ср) × zот × 24 / R₀ × A / 1000, кВт·ч/год.
   - Q_roof — то же для кровли.
   - q_spec = total_kWh / houseArea, кВт·ч/(м²·год).
   - Сравнение с нормой: 130 для 1-этажа, 90 для 2+ этажей (СП 50.13330 табл. 14).

**Где смотреть в PDF**: лист «Теплотехнический расчёт» (ПЗ.ТР), раздел 6.

**Затронутые файлы**:
- `packages/cc_engine/lib/src/thermal.dart` (+ 7 ClimateZone)
- `lib/services/thermal_explanation_pdf.dart` (новые методы `_climateForProject`, `_suggestInsulationThicknessMm`, `_energyPassport`)

---

### П.2 — Подбор сечения стропил по нагрузкам (СП 20 + СП 64)

**Стандарты**: СП 20.13330.2016 (снеговые/ветровые нагрузки), СП 64.13330.2017 (деревянные конструкции, проверка по моменту/прогибу).

**Изменения**:
1. **Новый класс `RafterSectionPicker`** (`lib/services/rafter_section_picker.dart`):
   - `pickFor(HouseProject project)` → `RafterPickResult`: считает все нагрузки и подбирает сечение из ряда [50×150, 50×200, 50×250, 75×250, 100×250].
   - `snowSgPerZone(int zone)` — Sg по СП 20 табл. 10.1 (зона 1 = 1.0, зона 8 = 5.6 кН/м²).
   - `snowMu(double angleDeg)` — коэффициент по уклону (α≤30°→1.0, α≥60°→0, линейно между).
   - `rafterStep(String roofingMaterial)` — шаг стропил (черепица 0.9 м, металл/мягкая 0.6 м, шифер 0.7 м).
   - `permanentLoadKnPerM2(roofingMaterial)` — постоянная g.
2. **Интеграция в КД-2 (лист «Стропильная система»)**:
   - В `pdf_additional_plans._paintRaftersSection()` хардкод заменён на вызов `RafterSectionPicker.pickFor(project)`.
   - В блоке спецификации показаны:
     - Снеговой район + Sg
     - μ для уклона + S₀ = Sg·μ
     - Постоянная g (кровля + обрешётка + утеп)
     - Расчётная нагрузка q = γg·g + γs·S₀ (кН/м²)
     - Линейная нагрузка q·шаг (кН/м)
     - **Проверка по СП 64.13330**: момент M (кН·м), напряжение σ (Н/мм²), прогиб f (мм), реакция R (кН).
     - Вердикт «Сечение проходит / требуется ферма / клееный брус» по сравнению с допускаемыми σ ≤ 13 МПа и f ≤ L/200.

**Где смотреть в PDF**: лист «КД-2 Стропильная система», блок «Нагрузки» и «Проверка по СП 64» в правой части листа.

**Затронутые файлы**:
- `lib/services/rafter_section_picker.dart` (новый)
- `lib/services/pdf_additional_plans.dart` (две точки интеграции — `_paintRaftersSection` и фасадный блок 3D)

---

### П.9 — Разбиение `pdf_builder.dart` на модули (безопасный рефакторинг)

**Цель**: уменьшить размер монолитного файла `pdf_builder.dart` (8571 строка) для облегчения навигации и снижения риска регрессий при правках.

**Что сделано (первый шаг)**:
- Создан новый файл `lib/services/pdf_builder_materials.dart` (704 строки), содержащий публичный класс `PdfBuilderMaterials` со статическими методами для:
  - **3D-рендера** (`renderBuilding3DToPdf`)
  - **Текстур стен на фасадах** (`paintWallTextureOverlay`)
  - **Текстур кровли** (`drawFacadeRoofTexture`)
  - **Заливок и подписей** (`facadeWallFill`, `facadeWallLabel`, `facadeRoofFill`, `facadeRoofLabel`, `wallMaterialCode`)
  - **Расходов материалов с отходами** (см. п.4) — `wallMaterialConsumption`, `wallMaterialElement`, `roofMaterialConsumption`, `insulationConsumption`
  - **Штриховки стен в разрезе** (`drawSectionWallHatch`)
- Перенесён вспомогательный приватный класс `_Pdf3DTri` в новый файл.
- Все 24 точки вызова в `pdf_builder.dart` обновлены: `_methodName(…)` → `PdfBuilderMaterials.methodName(…)`.
- Удалены неиспользуемые импорты `material_textures.dart` и `building3d.dart`.

**Результат**:
- `pdf_builder.dart`: было 8571 → стало 7888 строк (**−683 строки, ≈ 8 %**).
- Появился отдельный модуль для материалов/текстур/расходов — легче править без затрагивания остальной отрисовки.
- Тесты не сломаны: 14/14 passed.

**Что осталось «на потом»** (не входит в текущую сессию, но можно делать отдельно):
- Извлечь блок **отрисовки кровли** (`_paintRoofPlan`, `_drawRoofPolygon`, `_drawDashedPolygon`, `_drawDashedLine`, `_drawSlopeArrow`, `_drawSnowGuards`, `_drawRoofLegend`) — около 440 строк.
- Извлечь блок **отрисовки фундамента** (`_paintFoundationPlan`, `_paintFoundationSection`, `_foundationDraw*`) — около 1200 строк.
- Извлечь блок **отрисовки фасадов** (`_paintFacade`, `_paintFacadeAttachments`, `_drawFacadeWindow`, `_drawFacadeDoor`, ...) — около 1000 строк.
- Цель — довести `pdf_builder.dart` до ≤ 4000 строк, оставив там только оркестрацию и общие хелперы.

**Затронутые файлы**:
- `lib/services/pdf_builder_materials.dart` (новый)
- `lib/services/pdf_builder.dart` (удалены 683 строки, обновлены импорты и 24 вызова)

---

## Тестирование

| Шаг                          | Результат              |
|------------------------------|------------------------|
| `flutter analyze lib`        | 0 errors, 0 warnings   |
| `flutter test`               | 14/14 passed           |
| `flutter build web --release`| ✓ Built build/web      |
| `aws s3 sync` (Yandex YC)    | ✓ обновлено            |
| CDN purge `bc8rh3v5e5cz2p6phawl` | ✓ done             |
| `curl -sI https://konstruktor-stroenii.ru/` | HTTP 200 |

---

## Структура изменений по файлам

### Новые файлы (3)
- `lib/services/pdf_builder_materials.dart` (п.9)
- `lib/services/rafter_section_picker.dart` (п.2)
- `HANDOFF_v43.md` (этот документ)

### Изменённые файлы (5)
- `lib/services/pdf_builder.dart` (п.9 — удаление перенесённых методов, обновление вызовов; п.2/п.4 — косвенно через PdfBuilderMaterials)
- `lib/services/pdf_additional_plans.dart` (п.2 — интеграция RafterSectionPicker)
- `lib/services/thermal_explanation_pdf.dart` (п.1 — climate, авто-утепл, энергопаспорт)
- `lib/services/floor_plan_generator.dart` (п.3 — валидация проёмов)
- `packages/cc_engine/lib/src/thermal.dart` (п.1 — климатические зоны)
- `test/floor_plan_generator_test.dart` (п.3 — новый тест)

---

## Известные ограничения

- **П.9** — выполнен только **первый шаг** разбиения; в `pdf_builder.dart` остаётся ~7900 строк. Дальнейшая декомпозиция требует отдельной сессии и более тщательной проверки регрессий.
- **П.1 — энергопаспорт** показывает только укрупнённые годовые теплопотери (стены + кровля); пол, окна и вентиляция не учитываются (это потребует отдельного расширения с учётом Lуд/Aок/Aдв).
- **П.2 — расчёт стропил** опирается на упрощённую балочную схему (двухпролётная стропилина с равномерной нагрузкой); для сложной кровли (вальмовая, ломаная мансардная) нужно решение в специализированном расчётном модуле.
- Тесты сосредоточены на `floor_plan_generator` и `pdf_smoke_test`; для новых модулей (`thermal_explanation_pdf`, `rafter_section_picker`, `pdf_builder_materials`) специфических тестов нет — добавить в следующей итерации.

---

## Контакты / следующая сессия

Если нужно продолжить:
1. Декомпозиция `pdf_builder.dart` (кровля → фундамент → фасад) — см. «Что осталось» в п.9.
2. Добавить регрессионные тесты для каждого из 5 пунктов.
3. Расширить энергопаспорт на полы/окна/вентиляцию (СП 50 прил. Д).
4. Подключить расчёт ферм/клееных балок (СП 64 п. 8) для случая, когда `RafterSectionPicker.pickFor()` возвращает «не проходит».

Демо стабильно работает на https://konstruktor-stroenii.ru/.
